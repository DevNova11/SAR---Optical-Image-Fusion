__pycache__/
*.py[cod]
*.db
*.log
.env
.venv/
instance/
outputs/

# Real secrets only -- never commit a filled-in secrets.toml. The .example
# template (placeholder values, safe to commit) is intentionally not
# excluded by this pattern.
.streamlit/secrets.toml
